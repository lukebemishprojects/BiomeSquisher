plugins {
    alias libs.plugins.opensesame.loom
}

loom {
    mixin {
        useLegacyMixinAp = false
    }

    runs {
        configureEach {
            runDir "runs/${it.name}"
        }
    }
}
